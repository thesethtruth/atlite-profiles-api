services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: renewables-profiles:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    restart: unless-stopped
